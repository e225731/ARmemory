import SwiftUI

struct ContentView: View {
    var body: some View {
        StartView() // 最初の画面を「スタート画面」に設定
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}

